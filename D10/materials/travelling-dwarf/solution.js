console.log("WRITE YOUR SOLUTION IN HERE!");

var nextMoveX = 0

while (nextMoveX < 4) {
 d.move()
 nextMoveX = nextMoveX + 1;
}

d.orient('down');

var nextMoveY = 0;

while (nextMoveY < 16)  {
	d.move()
	nextMoveY = nextMoveY + 1;
}

d.orient('right');

var nextMoveX = 0

while (nextMoveX < 6) {
 d.move()
 nextMoveX = nextMoveX + 1;
}

d.orient('up');

var nextMoveY = 0;

while (nextMoveY < 11)  {
	d.move()
	nextMoveY = nextMoveY + 1;
}

d.orient('right');


var nextMoveX = 0

while (nextMoveX < 6) {
 d.move()
 nextMoveX = nextMoveX + 1;
}

d.orient('down');

var nextMoveY = 0;

while (nextMoveY < 9)  {
	d.move()
	nextMoveY = nextMoveY + 1;
}

d.orient('right');

var nextMoveX = 0

while (nextMoveX < 2) {
 d.move()
 nextMoveX = nextMoveX + 1;
}





